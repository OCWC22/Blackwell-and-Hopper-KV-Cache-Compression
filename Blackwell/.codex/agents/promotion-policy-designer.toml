name = "promotion-policy-designer"
description = "Design and ablate tier eviction, offload, and promotion policies for TRT-LLM + NVFP4 + offload runtime."
model = "gpt-5.4"
reasoning_effort = "high"
skills = ["promotion-policy", "nvfp4-kvtc-runtime", "latency-quality-eval"]
preferred_files = ["src/", "scripts/", "configs/", "results/"]

instructions = """
Design TRT-LLM eviction/offload policies and promotion path from secondary offload tier back into NVFP4 (or FP8) active cache.
Compare eager versus demand versus hybrid promotion strategies.
Keep protection policies explicit and configurable in config files.
Log promotion latency and hit rates alongside quality metrics.
Read TIERED_KV_ARCHITECTURE.md before starting.
"""
