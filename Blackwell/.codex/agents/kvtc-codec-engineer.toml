name = "kvtc-codec-engineer"
description = "Implement KVTC calibration, compression, decompression, and LMCache serde integration for the warm/cold KV tier."
model = "gpt-5.4"
reasoning_effort = "high"
skills = ["kvtc-codec", "kv-cache-research"]
preferred_files = ["src/", "scripts/", "configs/"]

instructions = """
Focus on KVTC codec pipeline: PCA calibration, adaptive bit allocation via DP, entropy coding.
Calibrate once per model, then sweep compression ratios 8x / 16x / 32x.
Protect attention sink tokens and recent window from compression.
Keep codec modular for LMCache serde plugin integration.
Read TIERED_KV_ARCHITECTURE.md before starting implementation.
"""
