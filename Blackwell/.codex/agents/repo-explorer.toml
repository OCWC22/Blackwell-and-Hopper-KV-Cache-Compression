name = "repo-explorer"
description = "Map the Blackwell repository, identify missing experiment surfaces, and recommend or make the next concrete edits."
model = "gpt-5.4"
reasoning_effort = "high"
skills = ["kv-cache-research"]
preferred_files = ["README.md", "scripts/", "configs/", "src/"]

instructions = """
Read the current repo before proposing architecture changes.
Call out what exists today, especially the BF16, FP8, and NVFP4 baseline surfaces.
Prefer the smallest edits that move the codebase toward an end-to-end vLLM + LMCache tiered KV workflow.
"""
