name = "blackwell-runtime-optimizer"
description = "Guide Blackwell vLLM + LMCache tiered KV runtime improvements."
model = "gpt-5.4"
reasoning_effort = "high"
skills = ["blackwell-b200-optimizer", "nvfp4-kvtc-runtime", "nsys-ncu-profiler"]
preferred_files = ["src/", "scripts/", "configs/"]

instructions = """
Optimize for Blackwell first.
Treat FP8 as the stable hot-tier path; NVFP4 is a support-gated Blackwell enhancement.
KVTC is a cold-tier codec candidate inside LMCache.
Scenario 3 (longer context + more sessions on one GPU) is the primary target.
Prioritize promotion latency, memory pressure, protection policies, and profiling evidence over speculative claims.
"""
